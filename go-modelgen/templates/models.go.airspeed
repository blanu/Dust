package $packageName

import (
  "github.com/blanu/Dust/go/Dust"
  #foreach($model in $models)
  "$packageName/$model"
  #end
)

func GetModels() map[string]Dust.Model {
  var models=make(map[string]Dust.Model)
  #foreach($model in $models)
  models["$model"]=${model}.New()
  #end
  return models
}
