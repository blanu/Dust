package $packagename

import (
  "testing"
)

func Test${name}Duration(t *testing.T) {
  var instance=&${name}{}
  var result=instance.WholeStreamDuration()
  if result<0 {
    t.Errorf("Negative duration %d", result)
  }
}
